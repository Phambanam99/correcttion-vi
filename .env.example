# Vietnamese Text Corrector - Environment Variables
# Copy file này thành .env và điền token của bạn

# HuggingFace Token (cần cho Vistral model)
# Lấy token tại: https://huggingface.co/settings/tokens
HF_TOKEN=hf_

# API Configuration
API_HOST=0.0.0.0
API_PORT=5000
